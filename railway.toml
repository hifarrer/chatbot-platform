[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn run:app --host 0.0.0.0 --port $PORT --timeout 120 --workers 1"
healthcheckPath = "/health"
healthcheckTimeout = 300
healthcheckInterval = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10 